import './App.scss';

import React, { FC } from 'react';

const App: FC = () => {
  return (
    <div className="app">
      <header className="app-header">React Calculator App</header>
    </div>
  );
};

export default App;
